[phases.setup]
apt_pkgs = ["python3", "python3-pip", "ffmpeg", "nodejs", "npm"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npm install -g pnpm", "pnpm install"]

[start]
cmd = "cd highrise_bot && python main.py"
